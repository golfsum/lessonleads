import type { Metadata } from "next";
import { SeoTopicPage } from "@/components/marketing/seo-topic-page";

export const metadata: Metadata = { title: "Golf Coach Website Widget", description: "Add an AI coaching assistant to an existing golf instruction website with one asynchronous script tag.", alternates: { canonical: "/golf-coach-website-widget" } };
export default function Page() { return <SeoTopicPage topic={{ eyebrow: "Website widget", title: "Add an assistant to your site without rebuilding it.", description: "One asynchronous script tag installs an isolated widget on WordPress, Squarespace, Wix, Webflow, or a custom site.", contextTitle: "A good widget should support the host site, not take it over.", context: "The LessonLeads loader creates an isolated iframe, avoids CSS conflicts, and loads after your page. It can float in a corner or embed inline on a dedicated ask-me page. The dashboard application itself is never shipped to your public website.", sections: [{ title: "Floating button", body: "A themed launcher — \u201cAsk Coach Mike\u201d — sits in the corner of every page. Clicking it opens the full widget with chat, lessons, videos, and swing upload." }, { title: "Inline embed", body: "Place the complete widget inside a lessons or get-help page. The host page keeps its own fonts, navigation, analytics, and booking links." }, { title: "Hosted page", body: "The same widget lives at a hosted URL you can share on Instagram, in emails, on Google Business Profile, and in QR codes." }], checklist: ["Load asynchronously with no impact on page speed", "Avoid global CSS and JavaScript collisions", "Open nearly full-screen on mobile with large touch targets", "Restrict the widget to your approved domains", "Keep private credentials on the server"], relatedHref: "/how-it-works", relatedLabel: "See the full setup flow" }} />; }
